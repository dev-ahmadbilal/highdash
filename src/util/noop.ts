/**
 * This method returns `undefined`.
 *
 * @returns Returns `undefined`
 *
 * @example
 * ```typescript
 * times(2, noop);
 * // => [undefined, undefined]
 * ```
 */
export function noop(): undefined {
  return undefined;
}
